<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- COLOR STYLE THEME-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlNormal">@android:color/white</item>
        <!-- You can also value colorControlNormal,  colorControlActivated, colorControlHighlight, jand colorSwitchThumbNormal.-->
    </style>

    <style name="SeekBarChooser" parent="Widget.AppCompat.SeekBar.Discrete">
        <item name="tickMark">@drawable/seek_bar_marker</item>
    </style>
</resources>
